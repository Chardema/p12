import React from 'react';
import styles from './RadarChart.module.scss';

const RadarChart = () => {
    return (
        <div className="radar-chart-container">
            <div className="chart-title">Top genres</div>
            <div className="chart">
                {/* ici va le code pour dessiner le graphique en radar */}
            </div>
        </div>
    );
};

export default RadarChart;
